import type { GameConfigUpdate } from "../game-config.types";

export const config512GameConfigProfile: GameConfigUpdate = {
  board: {
    gridSizeX: 512,
    gridSizeY: 512,
    cellSize: 30,
  },
};
