import type { GameConfigUpdate } from "../game-config.types";

export const config1024GameConfigProfile: GameConfigUpdate = {
  board: {
    gridSizeX: 1024,
    gridSizeY: 1024,
    cellSize: 30,
  },
};
