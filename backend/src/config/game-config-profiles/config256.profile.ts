import type { GameConfigUpdate } from "../game-config.types";

export const config256GameConfigProfile: GameConfigUpdate = {
  board: {
    gridSizeX: 256,
    gridSizeY: 256,
    cellSize: 30,
  },
};
